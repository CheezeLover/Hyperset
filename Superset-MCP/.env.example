# Superset connection settings
SUPERSET_BASE_URL=http://localhost:8088

# --- Auth mode: choose ONE ---

# Option A: Remote-user / header-based SSO (use with AUTH_TYPE = AUTH_REMOTE_USER in superset_config.py)
# Set this to the service-account username the MCP server will identify as.
# Leave blank to use username/password mode instead.
SUPERSET_REMOTE_USER=

# Option B: Classic username/password login
SUPERSET_USERNAME=admin
SUPERSET_PASSWORD=admin
